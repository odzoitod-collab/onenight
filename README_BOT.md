# OneNight Telegram Bot

Telegram бот для открытия веб-приложения OneNight как мини-приложения внутри Telegram.

## 🚀 Быстрый старт

### 1. Создание бота в Telegram

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка конфигурации

Отредактируйте файл `config.py` или установите переменные окружения:

```python
# В config.py
BOT_TOKEN = "ваш_токен_бота"
WEB_APP_URL = "https://ваш-домен.com"
```

Или через переменные окружения:
```bash
export BOT_TOKEN="ваш_токен_бота"
export WEB_APP_URL="https://ваш-домен.com"
```

### 4. Запуск бота

```bash
python telegram_bot.py
```

## 📱 Функциональность

- **Команда /start**: Показывает приветственное сообщение и кнопку для открытия приложения
- **Команда /help**: Показывает справочную информацию
- **Любое сообщение**: Предлагает открыть приложение

## 🔧 Настройка Web App

Для корректной работы мини-приложения ваш сайт должен:

1. Быть доступен по HTTPS
2. Иметь корректные CORS заголовки
3. Подключить Telegram Web App API

### Добавьте в ваш HTML:

```html
<script src="https://telegram.org/js/telegram-web-app.js"></script>
<script>
  // Инициализация Telegram Web App
  window.Telegram.WebApp.ready();
  
  // Расширение на весь экран
  window.Telegram.WebApp.expand();
  
  // Настройка темы
  document.body.style.backgroundColor = window.Telegram.WebApp.backgroundColor;
</script>
```

## 🌐 Деплой в продакшн

### Heroku

1. Создайте файл `Procfile`:
```
web: python telegram_bot.py
```

2. Установите переменные окружения в Heroku:
```bash
heroku config:set BOT_TOKEN=ваш_токен
heroku config:set WEB_APP_URL=https://ваш-домен.com
```

### Docker

```dockerfile
FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["python", "telegram_bot.py"]
```

## 📝 Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота | - |
| `WEB_APP_URL` | URL веб-приложения | - |
| `LOG_LEVEL` | Уровень логирования | INFO |
| `WEBHOOK_URL` | URL для webhook (продакшн) | - |
| `PORT` | Порт для webhook | 8443 |

## 🛠️ Разработка

Для разработки можно использовать polling режим (по умолчанию).
Для продакшна рекомендуется использовать webhook.

## 📞 Поддержка

При возникновении проблем проверьте:

1. Корректность токена бота
2. Доступность веб-приложения по HTTPS
3. Настройки CORS на сервере
4. Логи бота для диагностики ошибок